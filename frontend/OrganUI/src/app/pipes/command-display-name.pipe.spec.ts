import {CommandDisplayNamePipe} from './command-display-name.pipe';

describe('CommandDisplayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CommandDisplayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
